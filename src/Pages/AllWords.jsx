
const AllWords = ({ japan }) => {
    const { id, word, meaning, pronounciation, part_of_speech, difficulty, lesson_no, when_to_say, example } = japan;
    return (
        <div>

        </div>
    );
};

export default AllWords;